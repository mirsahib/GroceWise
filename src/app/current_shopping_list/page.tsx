import CurrentShoppingListContainer from "@/container/CurrentShoppingListContainer";
import React from "react";

type Props = {};

export default function CurrentShoppingList({}: Props) {
    return(
      <CurrentShoppingListContainer/>
    )
}
